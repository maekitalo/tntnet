<{

  std::string s = scallComp("subcomp", request);

  // we have the output of the subcomponent "subcomp" in s

  // just to demonstrate the use, we print it 10 times. It is not called 10
  // times, but just the output used 10 times.

}>
% for (unsigned i = 0; i < 10; ++i) {
   <$ s $><br>
% }
