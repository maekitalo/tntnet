<%pre>

#include <tntdb/connection.h>
#include <tntdb/connect.h>

</%pre>
<%config>

// read variable from tntnet.conf with default value
dburl = "sqlite:newsdb.db";

</%config>
<%thread>

// Create a persistent thread-scope variable and set up a connection.
tntdb::Connection conn(tntdb::connect(dburl));

</%thread>
